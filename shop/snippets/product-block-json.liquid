"index": {{ current_page | minus: 1 | times: 250 | plus: forloop.index }}, "handle": "{{ product.handle }}", "title": "{{ product.title | url_param_escape | replace: "'", "**quote**" }}", "vendor": "{{ product.vendor | url_param_escape }}", "published_at": "{{ product.published_at }}", "tags": [{% for tag in product.tags %} "{{ tag | url_param_escape }}" {% if forloop.last == false %},{% endif %}{% endfor %}], "vendor": "{{ product.vendor | url_param_escape | replace: "'", "**quote**" }}", "published_at": "{{ product.published_at }}", "tags": [{% for tag in product.tags %} "{{ tag | url_param_escape }}" {% if forloop.last == false %},{% endif %}{% endfor %}], "images": [{% for image in product.images %} { "src": "{{ image  | img_url: 'grande' }}" }{% if forloop.last == false %},{% endif %} {% endfor %}], "variants": [{% for variant in product.variants %} { "available": {{ variant.available }}, "compare_at_price": "{{ variant.compare_at_price | money_without_currency | url_param_escape }}", "price": "{{ variant.price | money_without_currency | url_param_escape }}", "taxable": {{ variant.taxable }}, "available": {{ variant.available }}, "title": "{{ variant.title | url_param_escape | replace: "'", "**quote**" }}" } {% if forloop.last == false %},{% endif %}{% endfor %}]